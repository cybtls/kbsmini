<!-- customTitleBar="{{true}}" titleBarOptions="{{titleBarOptions}}" -->
<page customTabbar="{{true}}" tabBarOptions="{{tabBarOptions}}">
  <!-- 我的账单 -->
  <my-bill slot="MyBill"></my-bill>
  <!-- 我的设置 -->
  <my-setting slot="MySetting"></my-setting>
</page>